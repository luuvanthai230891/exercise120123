// 6. Viết một hàm JavaScript nhận vào một chuỗi và trả về số lượng từ trong chuỗi.
const length = (string) => {
    return  string.length;
}

const string = "LuuVanThai";
console.log("Số lượng từ là: ",length(string) );